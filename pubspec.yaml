name: kurdish_translator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.5
  google_generative_ai: ^0.4.7
  http: ^1.6.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  record: ^6.2.0
  speech_to_text: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
